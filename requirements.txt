# ── Core GUI & video ─────────────────────────────────────────────────────────
customtkinter>=5.2
opencv-python>=4.8
pillow>=10.0

# ── Data ─────────────────────────────────────────────────────────────────────
pandas>=2.0
openpyxl>=3.1
numpy>=1.24

# ── AI / ML ──────────────────────────────────────────────────────────────────
# PyTorch: install the CUDA build matching your GPU *before* running pip install.
# See https://pytorch.org/get-started/locally/ — then uncomment the line below
# or let pip resolve a CPU-only version automatically.
# torch>=2.1
# torchvision>=0.16

ultralytics>=8.0
faster-whisper>=1.0
resemblyzer>=0.1
scikit-learn>=1.3

# ── Development / testing ────────────────────────────────────────────────────
pytest>=8.0
